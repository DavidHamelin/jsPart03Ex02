function showMessage() { // on déclare la fonction ici
    document.getElementById("text").style.display = "initial"; // display en css permet de modifier le type d'élément (block, inline, flex, initial ou none) 
}
function hideMessage() {
    document.getElementById("text").style.display = "none"; // ici l'élément disparait
}